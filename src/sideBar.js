/*! * TruckLoadCreator (https://github.com/morrisapps/TruckLoadCreator) * Copyright 2020 (c) Corey Morris * Licensed under MIT (https://github.com/morrisapps/TruckLoadCreator/blob/master/LICENSE.md) */var customers = [];var units = [];var unitIndex;var customerIndex;var canvasSide = new fabric.Canvas('d', {selection: false});var currentGroup;var currentColor;var currentFill;var currentCustomerName;var currentDrop;canvasSide.setHeight(126);canvasSide.setWidth(251);var grid = 25;var hasBundles = document.getElementById("hasBundle");let sideUnit = document.getElementById('sideUnit');function cutText(text, start, end) {    if (text.length <= end) {        return text;    } else {        return text.slice(start, end);    }}function unitText(custName, width, height, unitDrop, AE, text, rect, loc) {    let location = '';    if (loc.length > 0) {        location = '-' + loc;    }    if (width <= 24) {        text.set('text', '(' + unitDrop + ') ' + custName + " " + height + "\" " + AE + location);        while (text.width > rect.height) {            custName = cutText(custName, 0, custName.length - 1);            text.set('text', '(' + unitDrop + ') ' + custName + " " + height + "\" " + AE + location);            if (custName.length <= 0) {                break;            }        }    } else if (width <= 48 && width > 24) {        text.set('text', '(' + unitDrop + ') ' + height + "\" " + AE + location);        if (text.width > rect.width) {            if (AE.length > 4) {                AE = AE.slice(AE.length - 4, AE.length);            }        }        //custName set to empty string to correct font size change on small units with no customer        custName = '';        text.set('text', '(' + unitDrop + ') ' + height + "\" " + AE + location);    } else {        text.set('text', '(' + unitDrop + ') ' + custName + " " + height + "\" " + AE + location);        while (text.width > rect.width) {            custName = cutText(custName, 0, custName.length - 1);            text.set('text', '(' + unitDrop + ') ' + custName + " " + height + "\" " + AE + location);            if (custName.length <= 0) {                break;            }        }    }    //Set location to bottom if text is still too large    if (location.length > 0) {        if (text.width > rect.width) {            text.set('text', '(' + unitDrop + ') ' + custName + " " + height + "\" " + AE + "\n" + loc);            text.set('lineHeight', .75);            text.set('locationUnder', true);        }    }    text.set("styles", {        0: {            0: {fontSize: 15},            1: {fontSize: 15},            2: {fontSize: 15},            3: {fontSize: 15},            4: {fontSize: 15},            5: {fontSize: 15},            6: {fontSize: 15},            7: {fontSize: 15},            8: {fontSize: 15},            9: {fontSize: 15},            10: {fontSize: 15},            11: {fontSize: 15},            12: {fontSize: 15},            13: {fontSize: 15},            14: {fontSize: 15},            15: {fontSize: 15},            16: {fontSize: 15},            17: {fontSize: 15},            18: {fontSize: 15},            19: {fontSize: 15},            20: {fontSize: 15},            21: {fontSize: 15},            22: {fontSize: 15},            23: {fontSize: 15},            24: {fontSize: 15},            25: {fontSize: 15},            26: {fontSize: 15},            27: {fontSize: 15},            28: {fontSize: 15},            29: {fontSize: 15},            30: {fontSize: 15},            31: {fontSize: 15},            32: {fontSize: 15},            33: {fontSize: 15},            34: {fontSize: 15},            35: {fontSize: 15},            36: {fontSize: 15},            37: {fontSize: 15},            38: {fontSize: 15},            39: {fontSize: 15},            40: {fontSize: 15},            41: {fontSize: 15},            42: {fontSize: 15},            43: {fontSize: 15},            44: {fontSize: 15},            45: {fontSize: 15},            46: {fontSize: 15},            47: {fontSize: 15},            48: {fontSize: 15},            49: {fontSize: 15},            50: {fontSize: 15},        }    });    //Make Tag bold & font 17    for (let i = (text.text.length - AE.length - location.length); i <= text.text.length - location.length; i++) {        text.styles[0][i].fontWeight = 'bold';        text.styles[0][i].fontSize = 17;    }    //Make height bold & font 17    for (let i = (text.text.length - AE.length - location.length - height.toString().length - 2); i <= text.text.length - AE.length - location.length - 2; i++) {        text.styles[0][i].fontWeight = 'bold';        text.styles[0][i].fontSize = 17;    }    //Make customer font to 12    if (custName != '') {        for (let i = (unitDrop.length + 3); i <= (unitDrop.length + 3 + custName.length); i++) {            text.styles[0][i].fontSize = 12;        }    }    //Rotate text if height is greater than width    if (rect.height - 2 > rect.width) {        text.set('rotated', true);        text.rotate(90);    } else {        text.rotate(0);    }}function createUnit(width, height, cName, AE, color, fill, left, top, unitDrop, location, inCanvas) {    var custName = cName;    var rect = new fabric.Rect({        width: width * 2 - 4,        height: height * 2 - 2,        fill: fill,        stroke: color,        strokeWidth: 2,        originX: 'center',        originY: 'center',        rx: 5,        ry: 5    });    var text = new fabric.IText(/*custName + " " + height + " " + AE*/ '', {        height: height * 2 - 2,        textBackgroundColor: 'rgba(255,255,255,0.8)',        textAlign: 'center',        fontSize: 15,        fontFamily: 'Arial',        originX: 'center',        originY: 'center',    });    unitText(custName, width, height, unitDrop, AE, text, rect, location);    currentGroup = new fabric.Group([rect, text], {        unitHeight: height,        unitWidth: width,        left: left,        top: top,        //height: unitHeight,        selectable: true,        id: custName + AE,        unit: true,        remove: false,        hasControls: true,        customer: cName,        drop: unitDrop,        ae: AE,        intersects: true,        location: location,        inCanvas: inCanvas    });    if (currentGroup.item(1).locationUnder == true) {        currentGroup.set('height', currentGroup.height - 5);    }    currentGroup.hoverCursor = 'move';    currentGroup.moveCursor = 'grabbing';    //On deselect of units change color back to default to prevent edit border from staying    let tempActive = currentGroup;    tempActive.on('deselected', function (options) {        editOff(tempActive);    });}function AddSide(width, height, custName, AE, color, fill, unitDrop, location) {    createUnit(width, height, custName, AE, color, fill, 0, 0, unitDrop, location, true);    canvasSide.add(currentGroup);    canvasSide.centerObject(currentGroup);}currentFill = "white";// Random color currentColor = '#'+(0x1000000+(Math.random())*0xffffff).toString(16).substr(1,6);currentColor = 'black';//Adding objectsAddSide(+document.getElementById("width").value, +document.getElementById("height").value, document.getElementById("name").value, document.getElementById("ae").value, currentColor, currentFill, document.getElementById("drop").value, document.getElementById('location'));sideUnit.src = currentGroup.toDataURL();sideUnit.height = +document.getElementById("height").value;sideUnit.width = +document.getElementById("width").value;function getDropUnit(drop) {    //Checks if any unit exists with drop number. Returns first unit found.    var b;    var unit = 'none';    for (b = 0; b < units.length; b++) {        if (units[b].drop == drop) {            unit = units[b];        }    }    return unit;}function getDropCustomer(drop) {    var b;    var cust = 'none';    for (b = 0; b < customers.length; b++) {        if (customers[b].drop == drop) {            cust = customers[b];        }    }    return cust;}function updateDrop(cName) {    var error = "";    var i;    if (checkIfCustomerExists(cName)) {        if (document.getElementById("drop").value != "" && document.getElementById("drop").value > 0) {            currentDrop = document.getElementById("drop").value;        }        for (i = 0; i < customers.length; i++) {            if (customers[i].drop == currentDrop && customers[i].name != currentCustomerName) {                error = 'Drop Error';                break;            } else if (customers[i].drop != currentDrop && customers[i].name == currentCustomerName) {                error = 'Drop already exists'                break;            }        }        if (error != "") {            document.getElementById("drop").value = "";        } else {            customers[getCustomerIndex(cName)].drop = currentDrop;        }    }    return error;}function setDropCheck(cName) {    var cust = getCustomer(cName);    if (cust == null) {        //document.getElementById("drop").value = '';    } else if (cust.drop > 0) {        document.getElementById("drop").value = cust.drop;    }}function onDropChange() {    createSide();}function checkDropAssigned(newCustName, currentCust) {    if (currentCust.name != newCustName && currentCust != 'none') {        document.getElementById("drop").value = '';        return "Drop " + document.getElementById("drop").value + " is already assigned to " + currentCust.name + "\n";    } else {        return "";    }}function updateUnit() {    if (editing == true) {        let unUpdatedUnit;        if (editingUnit == null) {            unUpdatedUnit = canvas.getActiveObject();        } else {            unUpdatedUnit = editingUnit;        }        let dropError = '';        let oldFill = unUpdatedUnit.item(0).fill;        let oldColor = unUpdatedUnit.item(0).stroke;        let oldLeft = unUpdatedUnit.left;        let oldTop = unUpdatedUnit.top;        if (document.getElementById("drop").value != '') {            removeUnit(unUpdatedUnit);            unUpdatedUnit.set('remove', true);            let AddError = Add(+document.getElementById('width').value, +document.getElementById('height').value, document.getElementById('name').value, document.getElementById('ae').value, oldColor, oldFill, oldLeft, oldTop, document.getElementById('drop').value, document.getElementById('location').value, unUpdatedUnit.inCanvas);            if (AddError == '') {                updateHeightCount(canvas.getActiveObject());                canvas.remove(unUpdatedUnit);                updateHeightCount(unUpdatedUnit);                addCustomer(document.getElementById("name").value, document.getElementById("drop").value);            } else {                unUpdatedUnit.set('remove', false);                let rUnit = canvas.getActiveObject();                removeUnit(rUnit);                canvas.remove(rUnit);                updateHeightCount(rUnit);                addUnit(unUpdatedUnit);                canvas.add(unUpdatedUnit);                updateHeightCount(unUpdatedUnit);                addCustomer(unUpdatedUnit.customer, unUpdatedUnit.drop);            }        } else {            alert(dropError + 'Drop cannot be empty' + "\n");        }        editing = false;        editOff(null);        listUnits();        document.getElementById("unitContainer").style.borderColor = "#ccc";        document.getElementById('add').outerHTML = '<button style="margin-top: 20px;" class="tip expand" id="add" name="add" data-title="Add to List" onclick="AddUpdateButton()">Add</button>';        document.getElementById('location').value = '';        document.getElementById('ae').value = '';    }}function onChange() {    createSide();    if (editing != true) {        document.getElementById("unitContainer").style.borderColor = "#ccc";        document.getElementById('add').outerHTML = '<button style="margin-top: 20px;" class="tip expand" id="add" name="add" data-title="Add to List" onclick="AddUpdateButton()">Add</button>';    }}function onNameChange(list) {    currentCustomerName = document.getElementById("name").value;    let customer = getCustomer(currentCustomerName);    setBundleCheck(currentCustomerName);    if (customer == null) {        document.getElementById("drop").value = '';    } else {        document.getElementById("drop").value = customer.drop;    }    createSide();    if (list == true) {        listCustomer();    }}function createSide() {    canvasSide.remove(currentGroup);    currentCustomerName = document.getElementById("name").value;    AddSide(+document.getElementById("width").value, +document.getElementById("height").value, currentCustomerName, document.getElementById("ae").value, "black", "white", document.getElementById("drop").value, document.getElementById("location").value);    canvasSide.setHeight(currentGroup.height);    canvasSide.setWidth(+document.getElementById("width").value);    sideUnit.src = currentGroup.toDataURL();    sideUnit.height = currentGroup.height / 2;    sideUnit.width = currentGroup.width / 2;    canvasSide.requestRenderAll();}function onChangeSlider() {    document.getElementById("width").value = document.getElementById("swidth").value * 24;    document.getElementById("height").value = document.getElementById("sheight").value;    onChange();}function setBundleCheck(cName) {    if (checkIfCustomerExists(cName) && customers[getCustomerIndex(cName)].rack == true) {        hasBundles.checked = true;    } else {        hasBundles.checked = false;    }}function isBundlesChecked() {    var exists = false;    currentCustomerName = document.getElementById("name").value;    units.forEach(function (unit) {        if (unit.customer == currentCustomerName) {            exists = true;        }    });    if (hasBundles.checked == true) {        addCustomer(currentCustomerName, document.getElementById("drop").value);        customers[getCustomerIndex(currentCustomerName)].rack = true;    } else if (exists == false) {        customers[getCustomerIndex(currentCustomerName)].rack = false;        removeCustomer(currentCustomerName);    } else {        customers[getCustomerIndex(currentCustomerName)].rack = false;    }}function checkRack() {    let hasBundleCheckBox = document.getElementById("hasBundle");    let rackDrop = document.getElementById("drop").value;    let checkDrop = checkDropAssigned(document.getElementById("name").value, getDropCustomer(rackDrop));    if (rackDrop == '' && hasBundleCheckBox.value == true) {        alert("Drop cannot be empty or contain letters");        hasBundleCheckBox.checked = false;    } else if (checkDrop != '') {        alert(checkDrop);        hasBundleCheckBox.checked = false;    } else {        currentCustomerName = document.getElementById("name").value;        isBundlesChecked();        updateRack();        setBundleCheck(currentCustomerName);        canvas.requestRenderAll();    }}function getCustomer(name) {    let tempCust = null;    customers.forEach(function (customer) {        if (customer.name == name) {            tempCust = customer;            return tempCust;        }    });    return tempCust;}function isCustInTruck(cust) {    let inTruck = false;    let customer = getCustomer(cust);    if (customer != null && customer.rack == false) {        units.forEach(function (unit) {            if (unit.customer == customer.name && unit.inCanvas == true) {                inTruck = true;            }        });    } else {        inTruck = true;    }    return inTruck;}function getCustUnit(cust) {    var tempUnit = null;    units.forEach(function (unit) {        if (unit.customer == cust) {            tempUnit = unit;            return tempUnit;        }    });    return tempUnit;}function getIDUnit(id) {    let tempUnit = null;    units.forEach(function (unit) {        if (unit.id == id) {            tempUnit = unit;        }    });    return tempUnit;}function getTagUnit(tag) {    let tempUnit = null;    units.forEach(function (unit) {        if (unit.ae == tag) {            tempUnit = unit;        }    });    return tempUnit;}function sortUnit() {    units.sort(function (a, b) {        return a.drop - b.drop    });    units.reverse();    listUnits();}function addUnit(unit) {    if (unit.id != '') {        if (!checkIfUnitIDExists(unit.id)) {            units.push(unit);            document.getElementById("tUnits").innerText = units.length.toString();            sortUnit();        }    }}function removeUnit(unit) {    if (unit.id != '') {        if (checkIfUnitIDExists(unit.id)) {            units.splice(units.indexOf(units[unitIndex]), 1);            document.getElementById("tUnits").innerText = units.length.toString();            document.getElementById("drops").innerText = customers.length.toString();            sortUnit();        }        if (getCustUnit(unit.customer) == null) {            removeCustomer(unit.customer);        }    }}function sortCustomer() {    customers.sort(function (a, b) {        return a.drop - b.drop    });    customers.reverse();    listCustomer();}function addCustomer(cName, cDrop) {    if (cName != '') {        if (!checkIfCustomerExists(cName)) {            if (checkIfCustomerDropExists(cDrop) == '') {                customers.push({name: cName, rack: false, drop: cDrop});                document.getElementById("drops").innerText = customers.length.toString();                sortCustomer();                updateRack();            }        }    }}function removeCustomer(cName) {    if (cName != '') {        if (checkIfCustomerExists(cName)) {            if (customers[getCustomerIndex(cName)].rack == false) {                customers.splice(customers.indexOf(customers[getCustomerIndex(cName)]), 1);                document.getElementById("drops").innerText = customers.length.toString();                sortCustomer();            }        }    }}function checkIfUnitIDExists(id) {    unitIndex = 0;    //returnIndex disabled due to error with units existing after delete    let returnIndex = false;    while (unitIndex < units.length) {        if (units[unitIndex].id == id) {            return true;            //returnIndex = true;            break;        }        unitIndex++;    }    unitIndex = 0;    return false;    //return returnIndex;}function checkIfCustomerExists(cName) {    let i = 0;    let exists = false;    while (i < customers.length) {        if (customers[i].name == cName) {            exists = true;            break;        }        i++;    }    return exists;}function getCustomerIndex(cName) {    customerIndex = 0;    let index = null;    while (customerIndex <= customers.length) {        if (customers[customerIndex].name == cName) {            index = customerIndex;            break;        }        customerIndex++;    }    return index;}